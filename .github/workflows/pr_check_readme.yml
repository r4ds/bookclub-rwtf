name: pr_check
on:
  pull_request:
    branches: main
    paths:
      - 'README.md'
  workflow_dispatch:

jobs:
  bookdown:
    name: check_build
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required" '
